#include "main.h"
/**
 * mul - this multiploes two integers
 * @a: the first integer
 * @b: the second integer
 * Return: it returns the result
*/
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
